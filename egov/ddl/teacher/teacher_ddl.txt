CREATE TABLE `TB_TEACHER` (
  `SQ` int(10) unsigned NOT NULL AUTO_INCREMENT COMMENT '연번',
  `ENC_TEACHER_NAME` varchar(300) NOT NULL COMMENT '강사 이름',
  `CATEGORY_1` varchar(20) DEFAULT NULL COMMENT '분야 대분류(단일선택)',
  `CATEGORY_2` varchar(200) DEFAULT NULL COMMENT '분야 소분류(중복선택)',
  `CATEGORY_3` varchar(200) DEFAULT NULL COMMENT '대분류(서브)',
  `CATEGORY_ETC` varchar(20) DEFAULT NULL COMMENT '기타 분류',
  `MAJOR` varchar(20) DEFAULT NULL COMMENT '전공',
  `BIRTH` datetime DEFAULT NULL COMMENT '생년월일',
  `SEX` char(1) DEFAULT NULL COMMENT '성별',
  `ENC_EMAIL` varchar(300) DEFAULT NULL COMMENT '이메일',
  `ENC_PHONE` varchar(300) DEFAULT NULL COMMENT '휴대전화',
  `ENC_TELNO` varchar(300) DEFAULT NULL COMMENT '일반전화',
  `GUNGU` varchar(20) DEFAULT NULL COMMENT '주소지',
  `ENC_ADDRESS_DETAIL` varchar(500) DEFAULT NULL COMMENT '상세 주소',
  `ENC_SNS` varchar(500) DEFAULT NULL COMMENT '개인 SNS주소',
  `INTRODUCE` text DEFAULT NULL COMMENT '자기소개',
  `PROFILE_ORIGIN_NAME` varchar(100) DEFAULT NULL COMMENT '대표이미지 원본이름',
  `PROFILE_SAVE_NAME` varchar(100) DEFAULT NULL COMMENT '대표이미지 저장이름',
  `PROFILE_THUMB_NAME` varchar(100) DEFAULT NULL COMMENT '대표이미지 썸네일 이름',
  `PHONE_CHK` enum('Y','N') DEFAULT 'N' COMMENT '휴대전화 정보제공동의',
  `EMAIL_CHK` enum('Y','N') DEFAULT 'N' COMMENT '이메일 정보제공동의',
  `TEL_CHK` enum('Y','N') DEFAULT 'N' COMMENT '전화번호 정보제공동의',
  `ADDRESS_CHK` enum('Y','N') DEFAULT 'N' COMMENT '주소지 정보제공동의',
  `SNS_CHK` enum('Y','N') DEFAULT 'N' COMMENT '개인SNS주소 정보제공동의',
  `PROFILE_CHK` enum('Y','N') DEFAULT 'N' COMMENT '프로필 정보제공동의',
  `STATUS` enum('WAIT','ACCEPT','DENY') NOT NULL DEFAULT 'WAIT' COMMENT '상태',
  `REG_DATE` datetime NOT NULL COMMENT '등록날짜',
  `REG_ID` varchar(64) NOT NULL COMMENT '등록자',
  `UPD_DATE` datetime DEFAULT NULL COMMENT '수정날짜',
  `UPD_ID` varchar(64) DEFAULT NULL COMMENT '수정자',
  `DELETE_YN` enum('Y','N') NOT NULL DEFAULT 'N' COMMENT '삭제여부',
  `MB_ID` varchar(255) NOT NULL DEFAULT '' COMMENT '회원ID',
  `M_SQ` int(10) unsigned DEFAULT NULL,
  PRIMARY KEY (`SQ`)
) ENGINE=InnoDB AUTO_INCREMENT=314 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='강사'